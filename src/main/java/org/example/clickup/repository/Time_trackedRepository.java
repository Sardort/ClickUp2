package org.example.clickup.repository;

import org.example.clickup.model.Time_tracked;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Time_trackedRepository extends JpaRepository<Time_tracked, Integer> {
}
