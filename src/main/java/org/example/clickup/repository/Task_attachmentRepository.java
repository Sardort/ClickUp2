package org.example.clickup.repository;

import org.example.clickup.model.Task_attachment;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Task_attachmentRepository extends JpaRepository<Task_attachment, Integer> {
}
